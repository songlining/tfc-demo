# Local .terraform directories# Local .terraform directories

**/.terraform/***/.terraform/*



# .tfstate files# .tfstate files

*.tfstate*.tfstate

*.tfstate.**.tfstate.*



# Crash log files# Crash log files

crash.logcrash.log

crash.*.logcrash.*.log



# Exclude all .tfvars files, which are likely to contain sensitive data, such as# Exclude all .tfvars files, which are likely to contain sensitive data

# password, private keys, and other secrets. These should not be part of version *.tfvars

# control as they are data points which are potentially sensitive and subject *.tfvars.json

# to change depending on the environment.

*.tfvars# Ignore override files as they are usually used to override resources locally

*.tfvars.jsonoverride.tf

override.tf.json

# Ignore override files as they are usually used to override resources locally and so*_override.tf

# are not checked in*_override.tf.json

override.tf

override.tf.json# Include override files you do wish to add to version control using negated pattern

*_override.tf# !example_override.tf

*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan

# Include override files you do wish to add to version control using negated pattern*tfplan*

# !example_override.tf

# Ignore CLI configuration files

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan.terraformrc

# example: *tfplan*terraform.rc


# Ignore CLI configuration files
.terraformrc
terraform.rc

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Temporary files
*.tmp
*.temp